// Self-host compiler version, bumped as stages land (see BOOTSTRAP.md). Lives in
// its own file to prove the selfhost/ directory builds as one multi-file module.
const selfhostVersion: string = "0.1.0-stub"
