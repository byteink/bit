// Recursion: a guard clause stops the descent, then each call prints a star.
function stars(n: i64) {
  if (n <= 0) {
    return
  }
  print("*")
  stars(n - 1)
}

function main() {
  stars(6)
  print("\n")
}
