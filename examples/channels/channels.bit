// A buffered channel: send three values, then receive them back.
function main() {
  let nums = chan<i64>(3)
  nums <- 1
  nums <- 2
  nums <- 3
  let a = <- nums
  let b = <- nums
  let c = <- nums
  print("received ${a} ${b} ${c}\n")
}
