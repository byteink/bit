// Green threads: spawn a worker that produces a value, receive it over a channel.
fn producer(out: chan<i64>) {
  out <- 42
}

fn main() {
  let out = chan<i64>(0)
  spawn producer(out)
  print("worker produced ${<- out}\n")
}
