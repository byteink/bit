// Strings: concatenation with + and ${} interpolation.
fn greet(name: string): string {
  return "Hello, " + name + "!"
}

fn main() {
  print(greet("Bit"))
  print("\n")
  let n = 3
  print("n squared is ${n * n}\n")
}
