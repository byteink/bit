// Strings: concatenation with + and ${} interpolation.
function greet(name: string): string {
  return "Hello, " + name + "!"
}

function main() {
  print(greet("Bit"))
  print("\n")
  let n = 3
  print("n squared is ${n * n}\n")
}
