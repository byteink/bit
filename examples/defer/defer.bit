// defer: schedule a call that runs as the function returns, in LIFO order.
// Arguments are evaluated when `defer` runs, not when the deferred call fires.
fn main() {
  defer print("third\n")
  defer print("second\n")
  print("first\n")
}
