// defer: schedule a call that runs as the function returns, in LIFO order.
function step(s: string) {
  print(s)
}
function main() {
  defer step("third\n")
  defer step("second\n")
  step("first\n")
}
