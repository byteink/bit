// A struct with a value-receiver method.
struct Point { x: i64, y: i64 }

function (p: Point) manhattan(): i64 {
  return p.x + p.y
}

function main() {
  let p = Point{ x: 3, y: 4 }
  p.x = 10
  print("manhattan = ${p.manhattan()}\n")
}
