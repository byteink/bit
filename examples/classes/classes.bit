// A class with a value-receiver method.
class Point { x: i64, y: i64 }

fn (p: Point) manhattan(): i64 {
  return p.x + p.y
}

fn main() {
  let p = Point{ x: 3, y: 4 }
  p.x = 10
  print("manhattan = ${p.manhattan()}\n")
}
