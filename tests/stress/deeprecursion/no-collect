Pure recursion over fixed-size value-typed `[64]i64` locals — no `[]T` slice,
no heap allocation, no loop where a root is live. The thing under test is the
goroutine's fixed stack budget (`runtime/sched.zig`'s `stack_size`), not
rooting, so the BIT_GC=stress collector oracle has nothing to verify here.
