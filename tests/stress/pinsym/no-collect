Proves §11.9 pinned symbols: the evidence is the LINK — an undefined reference
to an exact byte-for-byte symbol name resolving against a Bit-defined pin. The
arithmetic afterwards is only a witness that the right code ran, so there is no
loop and no rooting claim to make.
