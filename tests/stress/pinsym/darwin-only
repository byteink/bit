SPEC 11.9's pin is target-independent, but this program PROVES it by resolving
the symbol through an `extern function` reference (SPEC 11.7), and that binds a
dylib symbol table Bit's fully static ELF output does not have. The stress
harness skips any directory containing this marker on a non-Darwin host. The
cross-target evidence for the pin itself is the duplicate-symbol probe on #1396.
