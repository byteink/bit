Marker read by tests/stress.zig: this program exercises runtime/alloc/linux,
whose page provider uses the Linux-only `syscall` primitive (SPEC 11.8), so a
non-Linux host skips it instead of failing. Its Darwin mirror is
tests/stress/allocdarwin.
