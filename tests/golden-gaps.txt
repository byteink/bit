# Golden corpus cases whose output from the shipped `bit` is KNOWN to differ
# from the checked-in `.expected`.
#
# THE GATE IS ON THE SET, NOT A COUNT (#1469). tests/bit/golden.bit fails when a path ENTERS
# this set (a new regression) AND when one LEAVES it (a gap that closed and was
# not deleted here). A swap at constant count is a failure, which is the point.
#
# Format: one case name per line, `name  # reason (#ticket)`. The reason is MANDATORY and must
# cite a smash ticket — the ticket is where the bug is tracked, and an entry that
# only says "known" is an excuse, not a gap.
#
# Deleting a line is how a gap is retired. That is the intended churn, and the
# gate tells you when it is due.
#
# DO NOT add a line to make a run green.

# EMPTY. Nothing is permitted to differ.
