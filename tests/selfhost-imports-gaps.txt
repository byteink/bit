# tests/imports/* projects the SELF-HOSTED `bit` cannot yet build or run
# correctly, one per line as `name  # reason`. Blank lines and full-line
# comments are ignored.
#
# tests/imports.zig builds and runs every project with BOTH compilers (#1484).
# The seed half must always pass. The self-hosted half is allowed to fail ONLY
# for a name listed here, and a listed name that SUCCEEDS fails the gate too —
# so a gap closing while another opens cannot cancel out at constant count.
# Deleting a line is how a gap is retired; that is the intended churn.
#
# Do not add to this list to make a build green. Every entry is a self-hosting
# defect with a ticket's worth of work behind it.

# selfhost/lower.bit: `len()` on a value whose type it cannot classify —
# std/http's h2connFill. One gap, eleven dependent projects.
fuzzparsers      # m8$h2connFill: lowerLen refuses the receiver's type
h3core           # m8$h2connFill
h3demux          # m8$h2connFill
http2conn        # m1$h2connFill
http2e2e         # m8$h2connFill
http2frames      # m1$h2connFill
http2hpack       # m1$h2connFill
http3e2e         # m9$h2connFill
http3qpack       # m9$h2connFill
httpsclient      # m8$h2connFill
nethttp          # m8$h2connFill

# selfhost/lower.bit: lowerToString has no case for the value's type.
seqtransform     # main: lowerToString refuses an interpolated value

# selfhost/lower.bit: a method call whose receiver type it cannot resolve,
# plus the lowerToString gap above.
nsimport         # roundTrip + main: unresolved method receiver, lowerToString

# selfhost/lower.bit: a member-access form (tuple/field) it cannot lower.
cryptobcrypt     # main: member access falls through lowerMember

# Builds, then MISCOMPILES — worse than a refusal, because it produces a binary.
coreopt          # selfhost binary exits 255
math             # selfhost prints e=4371299392 for the f64 const `e`
