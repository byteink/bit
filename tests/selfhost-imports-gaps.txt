# tests/imports/* projects the SELF-HOSTED `bit` cannot yet build or run
# correctly, one per line as `name  # reason`. Blank lines and full-line
# comments are ignored.
#
# `tests/bit/importsrun.bit` (the `test-imports-bit` step) builds and runs every
# project. A project is allowed to fail ONLY for a name listed here, and a
# listed name that SUCCEEDS fails the gate too — so a gap closing while another
# opens cannot cancel out at constant count. Deleting a line is how a gap is
# retired; that is the intended churn.
#
# The rule used to be "both compilers, and the seed half must always pass"
# (#1484). `seed/` was deleted in #1593 along with `tests/imports.zig`, so there
# is one half now and it is the shipped compiler's.
#
# Do not add to this list to make a build green. Every entry is a self-hosting
# defect with a ticket's worth of work behind it.


# Builds, then MISCOMPILES — worse than a refusal, because it produces a binary.
