// lint exit=0
// the same call, but the result is assigned straight back to the parameter
// it grew — the one shape the rule accepts as owning it (spec/LINT.md §4).
function catchEdgeArgs(buf: []byte): int {
  buf = append(buf, 1)
  return len(buf)
}
