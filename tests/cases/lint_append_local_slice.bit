// lint exit=0
// a locally-created slice is owned by this function outright — there is no
// caller backing array to alias, so append-aliasing must stay silent here
// even though the result is never assigned back.
function build(): int {
  let local = []byte(0)
  append(local, 1)
  return len(local)
}
