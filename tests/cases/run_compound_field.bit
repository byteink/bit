// run
// Read-modify-write lvalue lowering (#1712): compound-assign on a struct field.
struct P { n: int }

function main() {
  let p = P{n: 1}
  p.n += 5
  print("add=${p.n}\n")
  p.n -= 2
  print("sub=${p.n}\n")
}
