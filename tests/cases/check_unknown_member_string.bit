// error
// #2643: a member expression on a string receiver with no such field or
// method was accepted by `bit check` with exit 0, and only failed later, at
// lowering, with a spanless "cannot lower 1 function(s)" naming main. Sibling
// of check_unknown_member_primitive.bit (f64), check_unknown_member_i64.bit
// and check_unknown_member_bool.bit.
// Single-file golden: no prelude.
fn main() {
  let s: string = "a"
  let y: string = s.notAMethod
  println(y)
}
