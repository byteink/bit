// error
// E0048 is a WARNING, and `check` renders nothing unless the file also fails,
// so the shadowing alone is unobservable. The return mismatch is what makes the
// warning reachable by a golden at all — without it this case would be vacuous.
fn f(): i64 {
  let int = 1
  return "not an int"
}
