// types
interface Shape { area(): f64 }
class Circle { export r: f64 }
fn (c: Circle) area(): f64 { return 3.14159265358979 * c.r * c.r }
fn totalArea<T: Shape>(shapes: []T): f64 {
  let sum = 0.0
  for s of shapes {
    sum += s.area()
  }
  return sum
}
fn main() {
  let shapes = [Circle{ r: 1.0 }]
  let total = totalArea(shapes)
}
