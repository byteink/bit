// lint exit=1
// the aliasing bug this rule exists for (#1389): appending to a slice
// parameter without assigning the result back mutates the caller's own
// backing array, since append grows in place. Shape matches selfhost's
// catchEdgeArgs bug.
function catchEdgeArgs(buf: []byte): int {
  append(buf, 1)
  return len(buf)
}
