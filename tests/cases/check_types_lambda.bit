// types
function apply(f: (i32) => i32, x: i32): i32 { return f(x) }
function main() {
  let square = (n: i32) => n * n
  let compose = (g: (i32) => i32) => (m: i32) => g(g(m))
  let doubled = compose(square)
  let result = apply(square, 5)
}
