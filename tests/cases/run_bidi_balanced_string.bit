// run
// #2165, the ACCEPT half: a bidi override that OPENS and CLOSES within the
// same string literal is real, legitimate RTL text (the kind a UI string
// legitimately carries) and must not be flagged. Single-file golden: no
// prelude.
fn main() {
  let s = "‮مرحبا‬"
  print("${len(s)}\n")
}
