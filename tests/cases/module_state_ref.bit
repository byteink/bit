// error
// Module state may not hold a GC reference (§11.11). The collector never scans
// these cells, so a reference in one would dangle silently — this must be a
// compile error, not a runtime surprise.
let cache = "hello"
