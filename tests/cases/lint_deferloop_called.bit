// lint exit=0
// a defer inside a function merely CALLED from a loop is not reported —
// this rule is lexical scope only, never call-graph analysis.
function g() {
  defer print("x")
}
function f() {
  while (true) {
    g()
  }
}
