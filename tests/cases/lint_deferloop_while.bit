// lint exit=1
// a defer directly inside a while loop is reported: it runs at function
// exit, holding whatever it acquired for the whole rest of the function.
function f() {
  while (true) {
    defer print("x")
  }
}
