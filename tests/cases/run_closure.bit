// run
function makeAdder(n: i64): (i64) => i64 {
  return (x: i64) => x + n
}
function main() {
  let base = 10
  let add = (x: i64) => x + base
  let add100 = makeAdder(100)
  if (add(5) == 15 && add100(5) == 105) {
    print("closure ok\n")
  } else {
    print("wrong\n")
  }
}
