// lint exit=2
// a plain comment

// bit:lint max-nesting=6 -- reason
function f(a: int): int {
  let t = 0
  while (a > 0) { while (a > 0) { while (a > 0) { while (a > 0) { while (a > 0) { t = t + 1; a = a - 1 } } } } }
  return t
}
function main() { print("${f(0)}\n") }
