// run
class Point { x: i64, y: i64 }
fn (p: Point) sum(): i64 {
  return p.x + p.y
}
fn main() {
  let p = Point{x: 3, y: 4}
  p.x = 10
  if (p.sum() == 14) {
    print("sum 14\n")
  } else {
    print("wrong\n")
  }
}
