// run
// Read-modify-write lvalue lowering (#1712): `++`/`--` on a struct field.
struct P { n: int }

function main() {
  let p = P{n: 1}
  p.n++
  print("inc=${p.n}\n")
  p.n--
  print("dec=${p.n}\n")
}
