// lint exit=1
// bit:lint max-fn-lines=2 -- boundary case: an arrow function is measured too
// bit:lint disable unused-local -- fixture locals exist only to shape the arrow's line count
function outer() {
  let f = (x: int) => {
    let y = x
  }
}
