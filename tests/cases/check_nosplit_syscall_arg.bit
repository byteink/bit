// error
// SPEC 10.3.1 (#1640): a `syscall` is admitted, its ARGUMENTS are not. The
// allowlist still walks them, so an allocating argument stays E0075.
function nr(): i64 { return 9 }

@nosplit function f(): i64 {
  return syscall(nr(), 0)
}
