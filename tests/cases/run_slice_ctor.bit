// run
function main() {
  let a = []i64(3)
  a[2] = 5
  let b = []i64(2, 8)
  b = append(b, 1)
  print("a: len=${len(a)} cap=${cap(a)} a0=${a[0]} a2=${a[2]}\n")
  print("b: len=${len(b)} cap=${cap(b)}\n")
}
