// error
// #2642: a member expression on an i64 receiver with no such field or method
// was accepted by `bit check` with exit 0, and only failed later, at
// lowering, with a spanless "cannot lower 1 function(s)" naming main. Sibling
// of check_unknown_member_primitive.bit (f64), check_unknown_member_bool.bit
// and check_unknown_member_string.bit.
// Single-file golden: no prelude.
fn main() {
  let x: i64 = 1
  let y: string = x.toFixed(2)
  println(y)
}
