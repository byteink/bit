// error
// A numeric value does not convert to a string (§12.9) — `string(x)` is not a
// formatting call. The self-hosted checker admitted this and lowered it to a
// runtime segfault while the seed rejected it (#1490); the whole conversion
// family had no validator call site at all.
function main() {
  let x: i64 = 115
  print(string(x))
}
