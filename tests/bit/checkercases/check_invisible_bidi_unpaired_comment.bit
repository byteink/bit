// error
// #2165: an unpaired bidi override in a comment is the Trojan Source
// primitive (CVE-2021-42574) — it changes what a reviewer sees without
// changing what the source means, because a comment carries no semantics
// of its own. A BALANCED pair (real RTL text) is left alone: see the
// ACCEPT half, tests/cases/run_bidi_balanced_comment.bit.
fn main() {
  // ‮ reviewer sees this comment reversed from here on
  let s = "ok"
}
