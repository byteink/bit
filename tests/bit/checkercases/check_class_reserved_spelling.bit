// error
// #3446: `class` lexes to `Kind.KwStruct` (#3425), so every `parseFail` site
// that fell through to `describe(k)` -> `kindName(k)` for its "found" token
// named the wrong keyword — the caret sits on `class` but the text said
// `kw_struct`. #2248 fixed this only for identifier position (a keyword used
// as a binding/field/param name); expression position, exercised here, still
// fell through to the internal kind name. Type position has the identical
// defect and is not re-tested here (same fix, same call site).
fn main() {
  println(class)
}
