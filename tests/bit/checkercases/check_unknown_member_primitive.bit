// error
// #2641: a member expression on a PRIMITIVE receiver (f64 here) with no such
// field or method was accepted by `bit check` with exit 0, and only failed
// later, at lowering, with a spanless "cannot lower 1 function(s)" naming
// main. This diagnostic is newer than the pinned stage0 oracle, so it lives
// here rather than tests/cases/ (see tests/bit/checkerdiag.bit's header).
// Single-file golden: no prelude.
fn main() {
  let x: f64 = 1.0
  let y: string = x.toFixed(2)
  println(y)
}
