// error
// #3758: `cap()` with no arguments shares `vLenCapCall` with `len()` and had
// the identical silent-accept gap — `bit check` exited 0 and `lowerLen`
// (lowerprim.bit) panicked with "index out of range" while lowering. Pinned
// separately from check_zeroarg_len.bit because the two share the checker
// function but are reached by distinct call sites; #3756/#3755 both taught
// this repo that "the same function handles it" is not proof the other name
// was ever exercised.
fn main() {
  cap()
}
