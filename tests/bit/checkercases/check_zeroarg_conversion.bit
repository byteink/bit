// error
// #3758: a bare type-conversion call with no arguments — `int()` here — had
// NO arity check anywhere: `vRuntimeBuiltinArity`'s table only covers
// runtime-primitive function names, not conversion names, so `bit check`
// exited 0 and `lowerConvert` (lowerprim.bit) panicked with "index out of
// range" reading `args[0]`. `vConversionArity` now covers every predeclared
// numeric/`bool`/`string` conversion name (`primIndexByName`).
fn main() {
  int()
}
