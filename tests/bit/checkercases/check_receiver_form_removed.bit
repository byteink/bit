// error
// #3762: the written `fn (recv: Type) name()` method form was removed in
// 0.2.0 (SPEC §10.4) -- methods are now declared only inside the class body,
// using the implicit `this` receiver. A deleted form must still fail with a
// diagnostic that tells the user what to write instead, not just a generic
// parse-error cascade (compiler/parserdecl.bit's checkOldReceiverForm, E0103).
class Point {
  x: i64
  y: i64
}

fn (p: Point) sum(): i64 {
  return p.x + p.y
}

fn main() {
  print("hi\n")
}
