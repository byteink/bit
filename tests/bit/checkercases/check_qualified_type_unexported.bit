// error
// #2118: a namespace-qualified TYPE name that exists but is module-private
// (`json.Parser`, unexported in stdlib/json/parse.bit) must be rejected the
// same way an unexported value would be — a fix that makes qualified type
// resolution ignore visibility would leak module-private types across the
// import boundary §17.3 exists to enforce.
import json from "std/json"

fn use(p: json.Parser) {
}

fn main() {
}
