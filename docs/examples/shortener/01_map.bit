// 01: a code-to-URL map, printed.
// Demanded by: "store code to URL" -- maps, strings.
fn main() {
  let links = map<string, string>()
  links["abc123"] = "https://example.com"
  println("abc123 -> ${links["abc123"]}")
}
