// fib(40): naive recursion — measures function-call overhead.
function fib(n: i64): i64 {
  if (n < 2) {
    return n
  }
  return fib(n - 1) + fib(n - 2)
}

function main() {
  print("${fib(40)}\n")
}
