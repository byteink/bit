// Allocation churn: 20M short-lived heap structs, exercising allocator + GC.
struct Node { x: i64, y: i64 }

function main() {
  let n = 20000000
  let sum = 0
  let i = 0
  while (i < n) {
    let p = Node{ x: i, y: i + 1 }
    sum = sum + p.x + p.y
    i = i + 1
  }
  print("${sum}\n")
}
