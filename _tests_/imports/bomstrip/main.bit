// #2976: this file's first three bytes are a UTF-8 byte order mark
// (EF BB BF, spec/SPEC.md:68). Windows editors write one; the compiler must
// strip it before lexing begins, not fuse it onto whatever token follows.
fn main() {
  println("bom ok")
}
