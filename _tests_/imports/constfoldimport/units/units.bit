// #2115: the exporting side of the const-fold-across-an-import test. `KB` is a
// plain exported scalar `const`; `MB` is one whose own initializer is arithmetic,
// so the importer's fold has to recurse through the owner module rather than stop
// at a single literal.
export const KB: int = 1024
export const MB: int = 1024 * KB
