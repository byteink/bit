// The colliding declarations. Both parameter names are legal and both are used
// only inside their own bodies; nothing here may reach `main.bit`.

fn takesParam(helper: () => string): string {
  return helper()
}

fn takesAlias(MyInt: () => string): string {
  return MyInt()
}
