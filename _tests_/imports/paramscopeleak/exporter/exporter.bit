// The module `main.bit` imports from. Nothing here collides with anything; the
// collision is entirely inside the importing module, and it is the IMPORTED
// names that were captured by it.

export const limit: int = 7

export fn scale(n: int): int {
  return n * 3
}
