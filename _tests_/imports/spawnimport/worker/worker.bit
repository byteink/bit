// The spawned callee lives in its own module, so its definition is emitted as
// `m<id>$work`. A spawn site that names it from source text emits bare `work`.
export fn work(x: int, out: chan<int>) {
  out <- x * 3
}
export fn relay<T>(v: T, out: chan<T>) {
  out <- v
}
