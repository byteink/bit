Proves entryOf(f) (§11.10) yields a real code entry: a context is built as a
scheduler builds one and branched to. Straight-line by design — the whole proof
is that a single branch lands on taskBody's first instruction, and a loop around
it would test nothing further about the address.

This program's OWN code never reaches a safepoint (no loop, confirmed by
inspection), but `BIT_GC=stress` still does not report zero: 5/5 runs measured
`collections=101`, matching stress.bit's own documented scheduler-loop floor
(runtime/sched/workerrun.bit's `Worker.run` — see stress.bit's header). None of
those 101 fire at a back-edge this program emitted, so they still verify
nothing about this program's rooting; the reason to skip the corpus-wide
collection-count proof is unchanged.
