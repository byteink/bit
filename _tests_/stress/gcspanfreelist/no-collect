Drives its own private GC state (its own `gcBindMemory`/`gcInit`-bound arena) and
every assertion is about that private heap: how many chunks two successive
same-shape bursts cost the reserve, and whether the second is bounded by the
first's -- the #3868 span free list's whole point. Objects it allocates are
the private collector's and invisible to the ambient one.

CORRECTION: "None of it depends on whether the ambient (libbitrt) collector
ever runs" was true in spirit but understated. Measured, aarch64-macos, 5 runs
each policy: default `collections=0` every time; `BIT_GC=stress` 602666,
602642, 602639, 602015, 601716 (denominator: 5/5, spread ~0.16%, the largest
of the 7 files in this sweep — plausibly because this is also the longest-
running program of the 7, giving the scheduler's own timing more room to
vary), far above stress.bit's documented ~101 scheduler-loop floor.
Confirmed default and `BIT_GC=stress` stdout are both byte-identical to
`.expected`.

Unlike ../chanmakeroots/../chanroots/../gcindexchurn/../gcreserve (#4184),
this program's own final assertion closes the rooting question directly:
`arena`/`roots` are read again at the very end ("reached here so both stay
rooted across every ambient collection") and the result is printed as
`rooted`, so the arena's live interval genuinely extends through the last
ambient collection this program could see, and the printed value proves it
did.
