SPEC 11.8 syscall is Linux-only in this tree (pipe2/close/write raw traps);
Darwin publishes no stable syscall numbers, so this program neither compiles nor
runs on macOS. The stress harness skips any directory containing this marker on
a non-Linux host. Its Darwin mirror is _tests_/stress/sigpipedarwin.
