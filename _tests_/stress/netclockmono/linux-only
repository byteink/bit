imports runtime/net/linux directly to reach netClockNowNs (#3291), which issues
raw syscalls (SPEC 11.8) through parkMonoNs — rejected by the checker on Darwin,
same reason as ../netsocklinux. netClockNowNs on Darwin takes no scratch buffer
at all (runtime/net/darwin/tcp.bit), so there is no bug class to reproduce there.
