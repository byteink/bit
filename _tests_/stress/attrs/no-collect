Proves @naked / @nosplit EMISSION (SPEC §10.3.1), not rooting. @nosplit code is
defined by not polling for the collector, and the naked bodies are hand-written
asm with no safepoint at all. `spin`'s own `while` loop IS `@nosplit`
(deliberately, per the file's own header — "the back-edge takes no safepoint
poll"), so THIS PROGRAM'S loop reaches zero safepoints by construction; that
part of the original claim is correct and confirmed by reading `spin`'s
attribute.

`BIT_GC=stress` does not report zero for the whole PROCESS, though: 5/5 runs
measured `collections=101`, matching stress.bit's own documented
scheduler-loop floor (runtime/sched/workerrun.bit's `Worker.run`, which every
worker OS thread keeps running regardless of what this program's own
`@nosplit` code does). Its evidence is the linked binary's behaviour, not a
stack walk, and none of those 101 ambient collections happen inside `spin`,
`doubled`, `chain` or `two`, so none of them can affect what this program
proves.
