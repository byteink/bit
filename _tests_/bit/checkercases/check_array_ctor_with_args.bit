// error
// #4092: same gap as check_array_ctor_empty.bit, with a non-empty argument
// list — `[4]i64(1, 2, 3)` reached the same lowering backstop as the
// zero-argument form.
fn main() {
  let a = [4]i64(1, 2, 3)
  print("ok\n")
}
