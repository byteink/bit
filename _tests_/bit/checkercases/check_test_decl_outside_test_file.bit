// error
// #4127 (epic #4126, SPEC §19): a 'test' declaration PARSES in any '.bit'
// file -- the position-based grammar has no file check -- but is only
// VALID in one whose name ends '.test.bit'. This file does not, so the
// checker must reject it with a diagnostic naming that requirement, not a
// confusing parse error about an unexpected string literal.

test "decodes a null bulk string" {}
