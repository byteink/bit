// error
// #2165: a zero-width space typed directly into a string literal is
// invisible in review and indistinguishable from the ordinary space next
// to it. `\u{200B}` is the visible spelling and stays legal: see the
// ACCEPT half, _tests_/cases/run_invisible_char_escape.bit.
fn main() {
  let s = "a​b"
}
