// error
// #4091: `vSliceAllocArity` checked only the ARGUMENT COUNT, never the
// argument TYPE — `[]i64("x")` type-checked clean, ran, and produced a
// 1-element slice with no diagnostic anywhere. A string is never a valid
// slice length.
fn main() {
  let x = []i64("x")
  print("${len(x)}\n")
}
