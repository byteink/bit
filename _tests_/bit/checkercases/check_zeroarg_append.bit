// error
// #3758: `append()` with no arguments passed `bit check` silently (the
// checker's `vAppendCall` returned early on a zero-arg call with no
// diagnostic) and then panicked in `lowerAppend` (lowerprim.bit) with "index
// out of range" while lowering, since it reads `args[0]` unconditionally for
// the slice being appended to.
fn main() {
  append()
}
