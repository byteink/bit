// error
// Traits (SPEC §10.7, #3369): a trait may `use` another trait, but a cycle
// (A uses B, B uses A) is a compile error rather than an infinite
// composition.
trait A {
  use B
}
trait B {
  use A
}

class C {
  use A
}

fn main() {
  print("ok\n")
}
