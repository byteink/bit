// error
// #5162: `attr` is needed the moment a lowercase element exists at all
// (SPEC §12.12), whether or not it carries an attribute the reader wrote —
// an unbound `attr` must render at the ATTRIBUTE that needed it, not at its
// synthesized (past-EOF) position. `elem` is defined so the only report is
// the one this fixture pins.
fn elem(tag: string, ...kids: string): string {
  return tag
}
fn main() {
  let x = <div class="c" />
}
