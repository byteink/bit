// error
// #5155: `class` is accepted as a jsx_name (SPEC §12.12) ONLY inside a JSX
// tag's name/attribute position - the widening must not reach an ordinary
// binding name in the SAME file that has working JSX elsewhere (#4412's
// precedent: a predicate widened for one consumer reaching another).
fn elem(tag: string, ...kids: string): string {
  return tag
}
fn attr(name: string, value: string): string {
  return name
}
fn main() {
  let ok = <div class="x" />
  let class = 1
}
