// error
// #2118: a namespace-qualified TYPE name (`io.Bogus`) that does not exist on
// the imported module must still be rejected — a fix that makes `io.Anything`
// resolve would be worse than the parse-error bug it replaces. Same E0046
// export-table presence test `check_namespace_import_unexported.bit` already
// runs for a namespaced VALUE reference, reused for type position in
// validateattr.bit's `vQualType`.
import io from "std/io"

fn emit(w: io.Bogus) {
}

fn main() {
}
