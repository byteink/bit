// error
// #5162: a fragment desugars to a call to `frag` (SPEC §12.12); an unbound
// `frag` must render at the fragment itself, not at its synthesized
// (past-EOF) position. `text` is defined so the only report is the one this
// fixture pins.
fn text(s: string): string {
  return s
}
fn main() {
  let x = <>hi</>
}
