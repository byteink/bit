// error
// #3944: a lowercase JSX element desugars to `elem`'s variadic `Node`
// argument list (SPEC §12.12), so a `{expr}` child of the wrong type is the
// same diagnostic a mismatched variadic call argument gets, at the child's
// own span rather than the element's.
fn elem(tag: string, ...kids: string): string {
  return tag
}
fn attr(name: string, value: string): string {
  return name
}
fn main() {
  let n = 5
  let x = <div>{n}</div>
}
