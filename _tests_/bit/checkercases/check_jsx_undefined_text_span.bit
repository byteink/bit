// error
// #5162: a JSX text child desugars to a call to `text` (#5154, SPEC
// §12.12); an unbound `text` must render at the text child itself, not at
// its synthesized (past-EOF) position. `frag` is defined so the only report
// is the one this fixture pins.
fn frag(...kids: string): string {
  return ""
}
fn main() {
  let x = <>hi</>
}
