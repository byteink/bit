// error
// #2248: a reserved word (SPEC §5.2) used as a function parameter name.
// `from` is picked because it is the `import { x } from "..."` keyword and a
// natural name for a state-transition/range parameter (`move(from, to)`).
fn move(from: i64, to: i64): i64 { return to }

fn main() {}
