// error
// #4065: same gap as check_map_ctor_int_arg.bit, with a type-mismatched
// argument instead of a plausible-looking capacity hint — the checker never
// visited the call's argument list at all, so the argument's TYPE was never
// inspected either, only the map's own K/V generic parameters.
fn main() {
  let m = map<i64, i64>("x")
  m[1] = 7
  print("${len(m)}\n")
}
