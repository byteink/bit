// error
// #3839: naming a variadic parameter (SPEC §12.11, §10.3) — E0127. A
// variadic parameter takes zero or more arguments, so `xs = v` names no
// single slot.
fn sum(label: string, ...xs: int): int {
  return len(xs)
}

fn main() {
  print("${sum(label = "totals", xs = 5)}\n")
}
