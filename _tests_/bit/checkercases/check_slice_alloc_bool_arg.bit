// error
// #4091: same gap as check_slice_alloc_string_arg.bit — `[]i64(true)`
// type-checked clean and ran, silently reinterpreting a bool as a length. A
// bool is never a valid slice length.
fn main() {
  let x = []i64(true)
  print("${len(x)}\n")
}
