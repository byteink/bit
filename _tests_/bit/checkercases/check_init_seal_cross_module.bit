// error
// #3371: a class declaring 'init' may not be built with a composite literal
// outside its defining module (SPEC §12.2) — 'init's whole point is an
// unavoidable construction contract, and a literal from anywhere the fields
// are visible would guarantee nothing. `Account` is declared, WITH an
// 'init', in ./initsealsupport, a different module than this one.
import { Account } from "./initsealsupport"

fn main() {
  let a = Account{ balance: -500 }
}
