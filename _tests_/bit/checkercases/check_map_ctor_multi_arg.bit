// error
// #4064 changed this from #4065's original: `map<K,V>()` now has an arity to
// pattern-match against — zero or exactly one capacity hint, mirroring
// `chan<T>(...)`'s zero-or-one-capacity shape (vChanCtorArgs, validatezero.bit)
// rather than []T(n)'s mandatory length. Three arguments is still wrong-arity,
// but the message and code (still E0050) now say "zero arguments or one
// capacity hint" rather than "no arguments".
fn main() {
  let m = map<i64, i64>(1, 2, 3)
  m[1] = 7
  print("${len(m)}\n")
}
