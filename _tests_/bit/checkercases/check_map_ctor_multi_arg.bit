// error
// #4065: same gap as check_map_ctor_int_arg.bit, with three arguments —
// `map<K,V>()` has no arity to pattern-match against 1 or 2 the way
// `[]T(n)`/`[]T(n, m)` does (vSliceAllocArity, validatezero.bit); ANY
// non-empty argument list is rejected outright.
fn main() {
  let m = map<i64, i64>(1, 2, 3)
  m[1] = 7
  print("${len(m)}\n")
}
