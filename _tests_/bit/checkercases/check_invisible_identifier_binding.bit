// error
// #3465: an invisible code point inside an identifier renders identically
// to the same name without it, so two declarations that look like one line
// to a reviewer and to `git diff` bind two different symbols. That is
// strictly worse than the string/comment case E0097 already covers
// (#2165): there an invisible character hides DATA, here it creates a
// second *identity*. `ab` and `a<ZWSP>b` below are spelled to render
// identically. There is no escape syntax for an identifier, so the fix is
// simply to not type the character — see the ACCEPT half,
// _tests_/cases/run_nonascii_identifier.bit, for the legitimate non-ASCII
// case this must not reject.
//
// The bytes below ARE the raw ZERO WIDTH SPACE sequence (E2 80 8B), not the
// six characters `\`,`u`,`{`,`2`,`0`,`0` -- this file must be edited with a
// tool that preserves it, never retyped by hand.
fn main() {
  let a​b = 1
}
