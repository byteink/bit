// error
// #3758: `len()` with no arguments passed `bit check` silently (the checker's
// `vLenCapCall` returned early on a zero-arg call with no diagnostic at all)
// and then panicked in `lowerLen` (lowerprim.bit) with "index out of range"
// while lowering — a compiler crash on invalid input is not a rejection, so
// this pins a clean E0050 instead, the same shape #2998 pinned for a
// runtime-primitive builtin's wrong arity.
fn main() {
  len()
}
