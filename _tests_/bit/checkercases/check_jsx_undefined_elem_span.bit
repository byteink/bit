// error
// #5162: `elem` is a word the JSX desugaring needs (SPEC §12.12) but this
// module never wrote — it is appended past the real source for `identText`
// to spell, and an unbound-name diagnostic about it must render at the tag
// that needed it, not at that synthesized (past-EOF) position. `attr` is
// defined so the only report is the one this fixture pins.
fn attr(name: string, value: string): string {
  return name
}
fn main() {
  let x = <div class="c" />
}
