// error
// #3981: §12.9 has no implicit conversion between distinct numeric types, and
// `decimal` is a distinct numeric type — not a wider integer and not a float.
// An untyped CONSTANT still adapts (§15.4), which is what the last two lines
// assert by reporting nothing.
fn main() {
  let n: i64 = 7
  let d: decimal = n
  let back: f64 = d
  let trunc: i64 = d
  let okLit: decimal = 19.99
  let okInt: decimal = 3
  print("done\n")
}
