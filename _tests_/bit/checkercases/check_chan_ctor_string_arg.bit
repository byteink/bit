// error
// #4092: same gap as check_chan_ctor_multi_arg.bit, with a type-mismatched
// capacity argument instead — `lowerChanMake` handed the string straight to
// the runtime's `chan_make` as if it were an i64 capacity.
fn main() {
  let c = chan<i64>("x")
  print("ok\n")
}
