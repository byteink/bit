// error
// #2989: U+FEFF ZERO WIDTH NO-BREAK SPACE typed directly into a string
// literal, past offset 0, is NOT a byte order mark -- spec/SPEC.md:68 only
// exempts the file's true first character. It is invisible in review like
// every other zero-width character E0097 already rejects. `\u{FEFF}` is the
// visible spelling and stays legal; the leading-BOM half of this partition
// is _tests_/imports/bomstrip/main.bit (#2976).
//
// The bytes below ARE the raw EF BB BF sequence, not the escaped spelling --
// this file must be edited with a tool that preserves it, never retyped by
// hand.
fn main() {
  let s = "a﻿b"
}
