// error
// #4066: a `readonly` field rejects assignment from a FOREIGN module too —
// the guarantee must not depend on the caller being outside the declaring
// module (that half is check_readonly_field_assign_own_module.bit). `Stamp`
// is declared, with an exported readonly field, in ./readonlysupport, a
// different module than this one.
import { Stamp } from "./readonlysupport"

fn main() {
  let t = Stamp{ n: 5 }
  t.n = 6
}
