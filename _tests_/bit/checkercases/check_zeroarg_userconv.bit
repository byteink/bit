// error
// #3758: a user-declared type used in call position — `Point()` here — is
// equally a conversion (SPEC §12.9) and shared the exact same missing-arity
// gap as a predeclared conversion name: `bit check` exited 0 and
// `lowerConvert` (lowerprim.bit) panicked reading `args[0]`.
// `vConversionArity`'s `userTypeLookup` branch covers this shape.
class Point { x: i64, y: i64 }
fn main() {
  Point()
}
