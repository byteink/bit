// error
// #4148 (SPEC §19, epic #4126): the legacy shape-discovered test form is
// REMOVED. A bare top-level 'fn' with no parameters and no return type
// inside a '.test.bit' file is no longer silently discovered as a test (or
// silently treated as an ordinary helper) — it is a checker error, E0117,
// pointing the author at 'test "name" { }' instead.

fn looksLikeALegacyTest() {
  assert(true)
}
