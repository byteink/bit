// #5393 regression fixture, file A of this module (SPEC §17.1: one
// directory, one flat namespace with ../model.bit — same shape #5081 hit
// with `manyToMany`). `grouping` is an ordinary free function that happens
// to share its name with a field attribute declared in the sibling file,
// and happens to be the WRONG shape for one: an attribute reports by
// failing (SPEC §10.5), and this returns a plain `string`.
fn grouping(key: string): string {
  return key
}
