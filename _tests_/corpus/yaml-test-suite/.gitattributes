# Vendored fixture bytes are the test content itself. Without this, a
# checkout with core.autocrlf=input/true would silently rewrite any CRLF
# fixture to LF on `git add` (this repo's convention, matching
# _tests_/corpus/toml-test/.gitattributes) — verified on 2026-09-18 that none
# of these fixtures actually contain CRLF or invalid UTF-8, but this stays in
# place so a future re-vendor at a newer commit cannot be corrupted silently.
* -text
