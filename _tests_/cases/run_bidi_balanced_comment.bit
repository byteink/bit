// run
// #2165, the ACCEPT half: a bidi override that OPENS and CLOSES within the
// same comment is real, legitimate RTL text and must not be flagged — only
// an unpaired one is (tests/bit/checkercases/
// check_invisible_bidi_unpaired_comment.bit). Single-file golden: no
// prelude.
fn main() {
  // ‮مرحبا‬ a real right-to-left comment, balanced
  print("ok\n")
}
