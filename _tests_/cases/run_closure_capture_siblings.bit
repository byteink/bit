// run
// #2128: two closures over ONE binding share it with each other and with the
// frame — one cell per declaration, not one per closure. Was `get=0 n=0`.
fn main() {
  let n = 0
  let inc = () => { n = n + 1 }
  let get = () => n
  inc()
  inc()
  inc()
  print("get=${get()} n=${n}\n")
}
