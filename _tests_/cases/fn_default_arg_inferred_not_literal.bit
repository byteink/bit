// error
// #5258: `[]` is not a bare literal (Tag.SliceLit, not one of the tags
// vDefaultIsLiteral admits) -- E0159 already rejects it as a default in
// EITHER spelling, annotated or inferred, since that check runs before
// declaredness ever matters. Pinned so this stays true; no new diagnostic.

fn g(x = []) {}

fn main() {
  g()
}
