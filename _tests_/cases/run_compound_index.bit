// run
// Read-modify-write lvalue lowering (#1712): compound-assign on a slice index.
fn main() {
  let xs = []int{1, 2, 3}
  xs[0] += 5
  print("add=${xs[0]}\n")
  xs[0] -= 2
  print("sub=${xs[0]}\n")
}
