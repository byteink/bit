// error
// #3974: the fixed-array carve-out is type-conditional on the OPERAND, not a
// blanket admission of `len`/`cap` — a slice's `len` still lowers to a real
// SliceLen field read, so it stays E0075.
@nosplit fn f(s: []i64): int {
  return len(s)
}
