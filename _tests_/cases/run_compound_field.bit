// run
// Read-modify-write lvalue lowering (#1712): compound-assign on a class field.
class P { n: int }

fn main() {
  let p = P{n: 1}
  p.n += 5
  print("add=${p.n}\n")
  p.n -= 2
  print("sub=${p.n}\n")
}
