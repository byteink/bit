// run
fn identity<T>(x: T): T {
  return x
}
fn main() {
  let a = identity(41)
  let b = identity(true)
  if (b) {
    if (a == 41) {
      print("generic ok\n")
    } else {
      print("wrong\n")
    }
  } else {
    print("wrong\n")
  }
}
