// lint exit=0
// #1386: only ONE arm of this `if` diverges (there is no `else`), so the
// statements after it are still reachable and must not be reported.
fn f(x: bool): int {
  if (x) {
    return 1
  }
  let after = 2
  return after
}
