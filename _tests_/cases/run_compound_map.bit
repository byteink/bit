// run
// Read-modify-write lvalue lowering (#1712): compound-assign on a map entry.
fn main() {
  let m = map<string, int>{"a": 1}
  m["a"] += 5
  print("add=${m["a"]}\n")
  m["a"] -= 2
  print("sub=${m["a"]}\n")
}
