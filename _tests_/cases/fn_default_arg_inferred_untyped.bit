// error
// #5258: the inferred spelling has no declared type to assign `nil` against
// (contrast #5244's `x: T = nil`, legal for any nil-accepting T) -- `nil`
// passes E0159's literal check but has no default concrete type of its own
// (SPEC §15.4), so there is nothing for `x = nil` to infer.

fn f(x = nil) {}

fn main() {
  f()
}
