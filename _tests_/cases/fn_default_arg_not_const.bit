// error
// #5244: a default value must be a constant LITERAL (SPEC §10.3) -- #2905
// rejected call-site-evaluated defaults, and a compound expression is not one
// either.

fn f(x: int = 1 + 1): int {
  return x
}

fn main() {
  print("${f()}\n")
}
