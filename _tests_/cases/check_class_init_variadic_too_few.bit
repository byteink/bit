// error
// #4442: a variadic `init`'s fixed prefix is still mandatory — one fixed
// param plus the rest param, called with zero arguments.
class Foo {
  n: int
  init(prefix: string, ...args: string) { this.n = len(args) }
}
fn main() {
  let a = Foo()
}
