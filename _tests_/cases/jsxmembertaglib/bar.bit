// #5171's sibling module: `Bar` lives behind a module path so `<Lib.Bar />`
// is a genuine JSXMemberExpression tag, not a bare identifier.
import { Node, elem, attr, text } from "../../../pkg/web"

export fn Bar(label: string): Node {
  return elem("span", attr("class", "bar"), text(label))
}
