// error
// #3974: a map's `len` lowers to an `rt_call map_len`, which can allocate and
// reach a safepoint, so it stays E0075 even with the fixed-array carve-out.
@nosplit fn f(m: map<string, int>): int {
  return len(m)
}
