# 0.15.0 - repinned 2026-09-13, tag `v0.15.0` at 7eb45c63, the commit that
# regenerated the benchmarks on top of the batch pushed as 52be55dc. The full
# pre-push suite was green on 52be55dc (99 gates, 0 failed) and the only red was
# test-differentials, which is the standing lag this repin exists to clear.
#
# Verified before pinning, none of it inferred. All five assets fetched
# ANONYMOUSLY at 200 with digests matching dist/out/SHA256SUMS; the asset set
# derived from the published SHA256SUMS rather than counted. Release notes
# complete: 129 bullets against 129 non-merge commits since v0.14.0, and zero
# other-language names (checkNotesLanguage). brew 0.14.0 -> 0.15.0 with the
# installed binary answering `bit 0.15.0`. ghcr `0.15.0` and `latest` sharing
# index digest 58f52adf, both platforms having printed `toolchain ok` inside the
# build, and a LOGGED-OUT `docker run` printing `bit 0.15.0`. bitlang.org
# published from bit-website 44aaf11 with vendor/bit at v0.15.0, serving
# /packages/web/0.2.0/ and a docs/sql page carrying 14 references to symbols
# 0.14.0 cannot have.
#
# The shipped macOS binary was probed on a behaviour 0.14.0 LACKS rather than on
# one it shares: `@table class User { @id id: i64 @unique email: string }` plus
# `tableDescriptor()` printed `id:i64 attrs=1` / `email:string attrs=1`. That
# proves bin/bit and lib/libbitrt.a inside the tarball came from the same tree,
# which is the #2213 failure where six consecutive releases shipped a mismatched
# runtime and the smoke test could not see it.
#
# WHY THE MOVE. Seven differential constituents were red against the 0.14.0 pin,
# every one classified oracle lag on #4559 and none a miscompile: JSX
# (#3943/#3944/#3945), decimal (#3981/#3982 slice 1) and the #4968 trailing-arrow
# formatter rule, all landed after v0.14.0 was cut. Tonight's ORM work (#5051
# `@table`, #5054 `find<T>`) adds more syntax the old oracle cannot parse. The
# 0.14.0 pin could only fall further behind. Any differential red from here is a
# regression against 0.15.0, not lag.
711741aeaa576e9bc6ce5b4ea72b841e704630d03cc11eddf6a9ca5d0fa72cb6  bit-0.15.0-linux-aarch64.tar.xz
d30a6df2aa3290188b95c818e374603f4bd1c7e2a1064034ccc556baa3b959be  bit-0.15.0-linux-x86_64.tar.xz
9e18541ade61cdd9dd35eb1117f8442af5235e4897da141443fc06739f4e22eb  bit-0.15.0-macos-aarch64.tar.xz
5be377cb921f77e0ab752e9ac572fbba2b05b29eb06727f2711409c2dc4080ff  bit-0.15.0-windows-x86_64.zip
