# Stage0 digests, COMMITTED deliberately — see docs/release/bootstrap.md §1.
# Pinned stage0 version: 0.1.3. Changing stage0 means changing this file,
# which means a reviewable commit rather than a server-side change.
#
# Moved 0.1.2 -> 0.1.3 in #1593. The bump is load-bearing, not hygiene: 0.1.2
# CANNOT build today's compiler/ — `compiler/testgencheck.bit` calls
# `osRunTestBounded`, which 0.1.2 does not define, so a 0.1.2 stage0 fails with
# eight `error[E0040]: undefined name`. 0.1.3 defines it. That is what unblocks
# deleting seed/.
#
# Verified before pinning, on aarch64-macOS:
#   - the 0.1.3 artifact builds compiler/, and the result builds it again
#   - the chain converges: stage2 == stage3 byte-identical
#   - the published asset downloads ANONYMOUSLY and matches the digest below
#
# Compare bootstrap stages built to the SAME basename in DIFFERENT directories.
# A Mach-O ad-hoc signature embeds the binary's own file name, so `-o /tmp/stage2`
# vs `-o /tmp/stage3` differ at one byte range for that reason alone and look
# like a broken fixed point.
#
# EXACTLY ONE LINE PER TRIPLE. dist/stage0-verify.sh matches on the triple rather
# than the full filename (the version moves, the triple does not) and refuses
# outright when a triple has two digests. So the previous release's digests are
# REPLACED here, not appended — §3's diverse-double-compiling check gets the
# N-1 artifact from the releases page, not from this file.
06c43d37c9161dbd5db453bd4756cffa78b66b64ee51064dc98ca022da2e190e  bit-0.1.3-linux-aarch64.tar.xz
31b0bdfb4044fad10aa5781e6d3e60b0fbab2a02951e4ca333b67b5cea8aed28  bit-0.1.3-linux-x86_64.tar.xz
f221ea37b9db514e64e6f3c5cf2b020d11e7c83c17c8bc8e53c09621c6c6409f  bit-0.1.3-macos-aarch64.tar.xz
